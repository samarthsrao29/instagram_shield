// Copy this file to `config.local.js` (same folder) and fill overrides if needed.
// IMPORTANT: Never commit real secrets.

// Users should set their Groq key in Settings (stored in chrome.storage), not here.

// Optional override for Supabase (defaults are in `config.js`).
// globalThis.SUPABASE_URL = "https://xxxx.supabase.co";
// globalThis.SUPABASE_ANON_KEY = "eyJ...";
